import { 
    Navbar
} from 'reactstrap';

const Header = () => {
    return (
        <Navbar dark color='success' sticky='top' expand='md'>
            Music Player
        </Navbar>
    );
};

export default Header;